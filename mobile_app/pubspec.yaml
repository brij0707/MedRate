name: medrate
description: A medical residency verification and rating app.
publish_to: 'none'

# This section fixes the "Exit Code 65" by defining the Dart version
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  google_generative_ai: ^0.2.0
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
